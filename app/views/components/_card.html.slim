.card class=("card--#{variant}" if variant) id=id class=class_name
  - if title || actions.any?
    .card__header
      - if title
        h2 = title
      - if actions.any?
        .card__options = safe_join actions
  .card__body = content
  - if footer
    .card__footer = footer
